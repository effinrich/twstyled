---
"tw-styled": patch
"tw-styled-vite-plugin": patch
"tw-styled-cli": patch
"tw-styled-ui": patch
---

Bootstrap release automation with an initial valid changeset so the release workflow can open the first version PR after merge.
